<!--
Thanks for the PR! Quick checklist:

- Bumped version in `Cargo.toml` (CI rejects PRs that don't).
- Updated `CHANGELOG.md` under `## [Unreleased]`.
- `cargo fmt --all -- --check` passes.
- `cargo clippy --all -- -D warnings` passes.
- `cargo test --all` passes.
-->

## Summary

<!-- 1–3 sentences. What changed and why? -->

## User-visible changes

<!--
New flags, new behaviours, breaking changes, log/output format
shifts. Skip if purely internal.
-->

## Related

<!-- Link any related issues, ROADMAP items, or prior PRs. -->
